require 'spec_helper'

RSpec.describe Oct::Rubocop do
  it 'has a version number' do
    expect(Oct::Rubocop::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
